{
  "name": "amiusing",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy",
    "deploy": "wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
